export const selectIsDragging = (state) => state.drag.isDragging;

export const selectDropItems = (state) => state.drag.dropItems;

export const selectCurrentDrag = (state) => state.drag.current;

export const selectIsDropping = (state) => state.drag.isDropping;
